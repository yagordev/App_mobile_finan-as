import 'package:flutter/material.dart';

/// Paleta de cores moderna e clean para o projeto
class AppColors {
  // Fundos e Superfícies
  static const background = Color(0xFFF8F9FA);
  static const surface = Colors.white;
  
  // Cores Principais
  static const primary = Color(0xFF2D6A4F); // Verde mais profundo e moderno
  static const secondary = Color(0xFF40916C);
  static const accent = Color(0xFF52B788);
  
  // Cores de Status
  static const income = Color(0xFF2D6A4F);
  static const expense = Color(0xFFD90429); // Vermelho mais vibrante para despesas
  
  // Texto e Bordas
  static const textDark = Color(0xFF1B1B1B);
  static const textGrey = Color(0xFF6C757D);
  static const border = Color(0xFFE9ECEF);
  static const cardShadow = Color(0x0A000000);
}
